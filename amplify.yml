version: 1
#env:
#  variables:
#    key: value

frontend:
  phases:
    preBuild:
      commands:
        # source the script so it runs in the context of the current shell
        - . ./compute-hashes.sh
        - echo $SHOULD_NPM_INSTALL
        - npm ci
    build:
      commands:
        - echo "build goes here"

  artifacts:
    baseDirectory: ./dist
    files:
      - '**/*'

  cache:
    paths:
      - node_modules
#      - path

